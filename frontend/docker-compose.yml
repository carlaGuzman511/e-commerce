version: "3.9"

services:
  kiosko-frontend:
    container_name: kiosko-frontend
    build:
      context: .
      dockerfile: Dockerfile
    image: kiosko-frontend:latest
    ports:
      - "4200:80"
    restart: always
    volumes:
      - ./nginx-docker.conf:/etc/nginx/conf.d/default.conf:ro
